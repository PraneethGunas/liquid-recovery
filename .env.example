# ============================================================
# BIP39 Mnemonic Recovery - Configuration
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ============================================================

# REQUIRED: Your 10 known BIP39 words (space-separated)
# These are words 1-10 of your 12-word mnemonic
KNOWN_WORDS="word1 word2 word3 word4 word5 word6 word7 word8 word9 word10"

# OPTIONAL: Networks to scan (comma-separated)
# Available: liquid, bitcoin, testnet
NETWORKS=liquid

# OPTIONAL: Check addresses against blockchain API
# Set to "false" to only generate valid mnemonics without checking balances
CHECK_API=true

# OPTIONAL: Number of CPU workers (default: all available cores)
# NUM_WORKERS=8

# OPTIONAL: Addresses to derive per derivation path (default: 20)
# Increase if your wallet used many receiving addresses
ADDRESSES_PER_PATH=1

# OPTIONAL: Concurrent API checker threads (default: 8)
# API_WORKERS=8

# OPTIONAL: API rate limit in seconds between calls per thread (default: 0.05)
# Increase if you get rate-limited (HTTP 429)
# API_RATE_LIMIT=0.1

# OPTIONAL: Resume from mnemonic index (for interrupted runs)
# Check output/progress.json for the last_checked_index
# RESUME_FROM=0

# OPTIONAL: BIP39 passphrase (25th word / extra passphrase)
# Leave empty if you didn't set one when creating the wallet
# BIP39_PASSPHRASE=

# OPTIONAL: Blockstream API key (avoids rate limits)
# Get one free at: https://dashboard.blockstream.info
# API_KEY=

# OPTIONAL: Stop after finding first wallet (default: true)
# Set to "false" to exhaustively search all mnemonics
# STOP_ON_FIND=true

# ============================================================
# Local Elements Node (RPC mode) — NO rate limits!
# ============================================================
# Set CHECK_MODE=rpc to use a local Elements node instead of the API.
# Use ./run-local.sh to auto-start an Elements node via Docker.
# CHECK_MODE=rpc
# RPC_URL=http://elementsd:18884
# RPC_USER=liquid
# RPC_PASS=liquid
# ELEMENTS_CHAIN=liquidtestnet
